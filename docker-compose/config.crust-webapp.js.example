const domain = 'local.crust.tech'

window.CrustConfig = {
  system: {
    baseUrl: `https://system.api.${domain}`,
  },

  messaging: {
    baseUrl: `https://messaging.api.${domain}`,
  },

  crm: {
    baseUrl: `https://crm.api.${domain}`,
  },

  webapp: {
    baseUrl: `https://${domain}`,
    auth: {
      adtSignOutUrl: `https://satosa.didmos.${domain}/didmos/logout/`,
    },
  },
}
